@model Bricouli.Models.DevisRequest
@{
    ViewData["Title"] = "Supprimer un devis";
}

<div class="admin-form-card admin-fade">
    <h3>Confirmer la suppression</h3>
    <p>Voulez-vous supprimer la demande de <strong>@Model.Name</strong> ?</p>
    <form asp-area="Admin" asp-controller="DevisRequests" asp-action="Delete" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <div class="form-actions">
            <button class="btn-primary" type="submit">Supprimer</button>
            <a class="btn-secondary" asp-area="Admin" asp-controller="DevisRequests" asp-action="Index">Annuler</a>
        </div>
    </form>
</div>
